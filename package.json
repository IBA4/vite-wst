{
	"devDependencies": {
		"@sveltejs/vite-plugin-svelte": "^1.0.0-next.37",
		"autoprefixer": "^10.4.2",
		"postcss": "^8.4.6",
		"svelte": "^3.46.4",
		"tailwindcss": "^3.0.18",
		"vite": "^2.7.13",
		"vite-plugin-wasm-pack": "^0.1.9"
	},
	"scripts": {
		"wasm": "wasm-pack build ./wasm-crate --target web",
		"dev": "pnpm wasm && vite",
		"build": "pnpm wasm && vite build",
		"preview": "pnpm wasm && vite preview"
	}
}
